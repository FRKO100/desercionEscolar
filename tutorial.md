For using te database it will be neccessary to do the next:
1. Initialize xampp
- sudo ./xampp start
2. Initialize mysql.server:
- sudo /opt/lampp/bin/ ./mysql.server start
3. For reviewing changes, go to:
- /opt/lampp/bin
then, run:
- ./mysql -u <user> -p